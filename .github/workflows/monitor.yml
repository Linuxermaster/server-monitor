name: Monitor Servidor

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checar servidor
        env:
          SERVER_URL: ${{ secrets.https://www.app-relatorios.com.br }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.AAGZ_d-b3s1xmzg88qBttM65cgsiFuHqwfc }}
          TELEGRAM_CHAT_ID: ${{ secrets.8803676682 }}
        run: bash monitor.sh
